mat greating = "Give yor name: ";
text(greating);
mat name = input();

mat expected_name = "Mateusz";
mat condition = eq(name,expected_name);
if condition {
    mat resp = "Hello";
    text(resp);
    newline();
    text(name);
}
resp = "bye!";
newline();
text(resp);